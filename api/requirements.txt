# Lean dependency set for the Vercel FastAPI service (see vercel.json).
# lancedb/pyarrow/langgraph are intentionally excluded — they exceed the
# function bundle limit; the app degrades to direct Gemini extraction.
fastapi>=0.115.0,<1.0.0
httpx>=0.27.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0
google-genai>=1.0.0,<2.0.0
python-multipart>=0.0.9,<1.0.0
pydantic>=2.0.0,<3.0.0
